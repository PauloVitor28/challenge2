package map;

import org.openqa.selenium.WebDriver;
import pages.BasePage;

public class HeadersMap extends BasePage {
    public HeadersMap(WebDriver driver) {
        super(driver);
    }
}
